DATE: April 18th 2026
AUTHOR: MiguelAGDev
ACTIONS: Create Repositare and Install dependencies


Description: 

- BACKEND (npm install)
	*express - framework that handles routes and http request. This is the server
	* mysql2 - driver that connects Node to MySQL databases
	*bcryptjs - encrypts passwords. It never store password in plain text
	*jsonwebtoken - generates JWT tokens to mantein the user session without storing anything in the server
	*multer - middleware that handles file uploads (PDFs)
	*cors - allows frontend (port 5173) to communicate with the backend (distinct port) without the browser blocking
	*dotenv - lets you store sensitive files (DB password, JWT secred) in a .env file whithout pushing them to GitHub


- FRONTEND (npm install)
	*vite + react - Vite is the moder bundler that replace Create React App. Start very fast.
	*axios - makes HTTPS request to your backend. Cleaner than native fetch.
	*react-router-dom - handles frontend routes (/login,/home,/upload)
XAMPP (already installed):

- XAMPP 
	*a local development environment that includes Apache and MySQL. We use it to run a MySQL database server locally without any cloud setup. The database is managed visually through phpMyAdmin, accessible at http://localhost/phpmyadmin.

- GITHUB 
	*i create the repository and align in the correct branch (main)


DATE: 20th April 2026
AUTHOR: MiguelAGDev
ACTIONS:

